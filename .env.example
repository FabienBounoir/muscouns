# ================================================================
# Muscouns – Variables d'environnement
# Copier ce fichier en .env et renseigner les valeurs
#   cp .env.example .env
# ================================================================

# --- MongoDB -------------------------------------------------------
# URI de connexion complète (inclut user:password si auth activée)
# Exemple local  : mongodb://localhost:27017
# Exemple Atlas  : mongodb+srv://<user>:<password>@cluster.mongodb.net
MONGO_DB_URI=mongodb://localhost:27017

# Nom de la base de données
MONGO_DB_NAME=muscouns

# --- JWT -----------------------------------------------------------
# Clé secrète utilisée pour signer les tokens d'authentification
# Générer une valeur forte : openssl rand -hex 64
JWT_SECRET=change_me_with_a_strong_random_secret
